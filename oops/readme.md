-class always have first letter as capital to differenciate from function
-class name, constructor, attributes, methods, instantiation
-self keyword refers to that object.
-accessing attributes and methods from object
- _amt   this means single underscore _ means this value can be accessed but don't change just for information
- __amt this means this is very sensitive so this cannot be accessed outside.



=>assignment. ===== there's another process for declaring object find that. 

#encapsulation
